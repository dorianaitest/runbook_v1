# Shared PostgreSQL instance (pgvector/pg17)
# Used by: LiteLLM (litellm db), n8n (n8n db), FastAPI audit logs (fastapi db)
# Each service gets its own database and user — see infra/postgres/init/ for setup scripts.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DB=postgres
